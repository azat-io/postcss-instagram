{
  "name": "postcss-instagram",
  "version": "1.0.0",
  "description": "PostCSS plugin for using Instagram filters in CSS",
  "main": "index.js",
  "repository": "git@github.com:azat-io/postcss-instagram.git",
  "author": "Azat S. <to@azat.io>",
  "license": "MIT",
  "scripts": {
    "lint": "eslint . --ignore-path .gitignore",
    "lint-staged": "lint-staged",
    "test": "yarn run lint"
  },
  "pre-commit": "lint-staged",
  "lint-staged": {
    "*.js": "lint",
    "*.{md,js}": "yaspeller-ci"
  },
  "dependencies": {
    "postcss": "^6.0.6"
  },
  "devDependencies": {
    "coveralls": "^2.13.1",
    "eslint": "^4.1.1",
    "eslint-config-postcss": "^2.0.2",
    "jest": "^20.0.4",
    "lint-staged": "^4.0.1",
    "pre-commit": "^1.2.2",
    "yaspeller-ci": "^0.6.0"
  }
}
